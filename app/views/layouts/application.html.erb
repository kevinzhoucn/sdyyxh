<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : @page_title || t('.title') %></title>

    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body style="background-color:#f5f5f5">
    <div class="container" style="background-color:white;padding:10px">
        <div class="masthead" style="margin-top:20px;">
          <h3 class="muted"><a href="/" style="color:red"><%= t('.title') %></a>
            <% if user_signed_in? %>              
              <%= link_to '退出管理', destroy_user_session_path, :method => :delete, :style=>"color:blue;margin-right:10px;font-size:50%;float:right" %>
              <a href="/front/admin" style="color:blue;margin-right:10px;font-size:50%;float:right">网站管理</a>
            <% else %>
              <a href="/login" style="color:red;margin-right:10px;font-size:50%;float:right">登录</a>
            <% end %>
          </h3>
          <div class="navbar">            
            <div class="navbar-inner" style="background-image:url(/assets/bg-nav.png);background-repeat:repeat;border:none">
              <div class="container">
                <ul class="nav color-white">
                  <%= render :partial => 'layouts/common/navlist' %>
                </ul>
              </div>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="span12" style="background-color: white">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>      
        </div><!--/row-->
     
      <footer>
        <p>&copy; 演出协会 2013</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
